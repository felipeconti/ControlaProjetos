<%- model_class = Customer -%>

<div class="page-header">
  <h3>
    <%= image_tag @customer.logo.url(:small) %>
    <%= link_to "Todos Clientes ", root_path, class: 'small' %> \ <%= getNameOrNick(@customer.name, @customer.nickname) %>
  </h3>
</div>

<p id="notice"><%= notice %></p>

<%= link_to "Novo Projeto", new_customer_project_path(@customer), class: 'btn btn-primary' %>

<%= render :partial => 'projects/list' %>
