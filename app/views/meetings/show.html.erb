<%- model_class = Meeting -%>

<div class="page-header">
  <h1><%= link_to "Todas Reuniões> ", meetings_path, class: 'small' %> <%= @meeting.title %> </h1>
</div>

<p id="notice"><%= notice %></p>

<%= link_to "Novo Item", new_meeting_meeting_item_path(@meeting), class: 'btn btn-primary' %>

<%= render :partial => 'meeting_items/list' %>